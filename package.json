{
  "name": "fedex-prism",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "scripts": {
    "dev": "turbo build & turbo dev",
    "build": "turbo build",
    "build:browser": "turbo build --filter=@fedex-prism/vis-browser...",
    "typecheck": "tsc -b",
    "test": "turbo test",
    "clean": "turbo clean",
    "preinstall": "npx only-allow pnpm",
    "generate": "turbo gen package"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^12.4.1",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "turbo": "^2.10.12",
    "typescript": "^6.0.3"
  }
}
